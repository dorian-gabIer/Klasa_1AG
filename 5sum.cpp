#include <bits/stdc++.h>
using namespace std;
int main()
{
    int temp, sum = 0;
    while(cin >> temp)
    {
        if(temp == 0) break;
        sum = (sum) + (temp);
    }
    cout << sum;
    return 0;
}
